<div class="mx-auto md:w-2/3 w-full">
  <h1 class="font-bold text-4xl"><%= t('views.applicants.edit_applicant')%></h1>

  <%= render "form", applicant: @applicant %>

  <%= link_to t('views.applicants.button.show'), @applicant, class: "ml-2 rounded-lg py-3 px-5 bg-gray-100 inline-block font-medium" %>
  <%= link_to t('views.applicants.button.back'), applicants_path, class: "ml-2 rounded-lg py-3 px-5 bg-gray-100 inline-block font-medium" %>
</div>
